# No SPA rewrite rules.
# NEFES currently only serves the root route `/`.
# A blanket `/* -> /index.html 200` rule triggers Cloudflare Pages
# "Infinite loop detected" and is not required for this deployment.
