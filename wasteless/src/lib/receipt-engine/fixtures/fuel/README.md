# Fuel fixtures

Architecture samples for dispensed-fuel receipts (liters × TL/L).

| Fixture | Notes |
|---------|--------|
| `shell-motorin.txt` | Synthetic Shell-style motorin line + card slip |

Sold quantity pattern: `29,766 LT x 79,17` (not a package attribute).
Card-slip lines (`AID`, `TERM`, `ONAY`, `PAYWAVE`) must stay out of PRODUCTS.
