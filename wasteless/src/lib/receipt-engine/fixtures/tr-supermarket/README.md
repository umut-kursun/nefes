# Deprecated fixture path

Supermarket fixtures migrated to `../supermarket/`.
Weighted continuation moved to `../weighted/`.
Footer payments moved to `../multi-payment/`.

Use `fixtureRegistry.ts` — do not add new fixtures here.
